<UserControl x:Class="DeviceSense.UI.Views.Storage.StorageOverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F0F2F5">
    <Grid Margin="32,28,32,32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Storage" FontSize="24" FontWeight="Bold" Foreground="#1E1E2E" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1" Text="Disk drives and space usage" FontSize="13" Foreground="#9E9EBB" Margin="0,0,0,20"/>
        <Border Grid.Row="2" Background="#E8E8F0" CornerRadius="8" Padding="16,10" Margin="0,0,0,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Drive" FontSize="12" FontWeight="SemiBold" Foreground="#6B6B8A"/>
                <TextBlock Grid.Column="1" Text="Total" FontSize="12" FontWeight="SemiBold" Foreground="#6B6B8A"/>
                <TextBlock Grid.Column="2" Text="Free" FontSize="12" FontWeight="SemiBold" Foreground="#6B6B8A"/>
            </Grid>
        </Border>
        <ListView Grid.Row="3" ItemsSource="{Binding Drives}" Background="Transparent" BorderThickness="0">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,2,0,0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <Border Background="White" CornerRadius="8" Padding="16,12">
                                    <ContentPresenter/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Name}" FontSize="13" Foreground="#1E1E2E" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="{Binding TotalBytes}" FontSize="13" Foreground="#6B6B8A" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="{Binding FreeBytes}" FontSize="13" Foreground="#22C55E" VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
